{
  "name": "web3-browser-extension",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "watch": "vite build --watch",
    "dev": "vite build --watch",
    "test": "esbuild test/verification-flow-proof.ts --bundle --platform=node --format=esm --outfile=test/.verification-flow-proof.mjs --log-level=error && node test/.verification-flow-proof.mjs"
  },
  "dependencies": {
    "@a16z/helios": "^0.11.1",
    "ethers": "^6.13.0",
    "hyparquet": "^1.26.1",
    "hyparquet-compressors": "^1.1.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
